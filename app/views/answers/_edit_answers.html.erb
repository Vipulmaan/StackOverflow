<%= form_for(@answer, url: user_question_answer_path(@user.id, @answer.question_id, @answer.id)) do |f| %>

  <%= f.label :Answer %>
  <%= f.text_field :description, class: 'form-control' %>
  <%= f.submit "update my answer", class: "btn btn-primary" %>
<% end %>