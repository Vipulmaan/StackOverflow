<h1>Questions </h1>

<% @questions.each do |question| %>
  <p>
    <%= link_to question.title, user_question_path(question.user_id, question.id) %>   <br>

  </p>
<% end %>

<%= link_to "Ask Question", new_user_question_path(@question.user_id) %>

