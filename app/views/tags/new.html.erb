<%= render "layouts/navbar" %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@tag, url:  user_tags_path(current_user.id)) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>
      <%= f.submit "add tag", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>