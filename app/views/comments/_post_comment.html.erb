<br>
<%= form_for(@comment, url: user_question_comments_path(@user.id, @question.id)) do |f| %>

  <%= f.label :comment %>
  <%= f.text_field :description, class: 'form-control' %>
  <%= f.submit "post my comment for question ", class: "btn btn-primary" %>
<% end %>

<% if @comment.errors.any? %>
      <% for message_error in @comment.errors.full_messages %>
        <li> <%= message_error %></li>
      <% end %>
    </ul>
  <% end %>