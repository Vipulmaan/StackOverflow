<br>
<%= form_for(@comment, url: user_question_answer_comment_path(@comment.user_id, Answer.find(@comment.commentable_id).question_id, @comment.commentable_id, @comment.id)) do |f| %>

  <%= f.label :comment %>
  <%= f.text_field :description, class: 'form-control' %>
  <%= f.submit "update my comment for answer ", class: "btn btn-primary" %>
<% end %>